import scala.collection.mutable.ArrayBuffer

/**
 * @author CuTs
 */
class genericQueue {
var queueBody = new ArrayBuffer

def enqueue[T] (item: T) {}
def dequeue {}
def isEmpty = queueBody.isEmpty
def toString = queueBody.toString()
  
}