trait IThermometer {
  // = avg degrees Farenheit
  def getMeanTemperature(cities: List[String]): Double
}

class CenTherm {
// = degrees Centigrade
  def computeTemp(city: String) = {
    
    
  }
}

object CenTherm {
def apply(city: String) = {
  val temp = new CenTherm
  temp.computeTemp(city)
}  
  
}