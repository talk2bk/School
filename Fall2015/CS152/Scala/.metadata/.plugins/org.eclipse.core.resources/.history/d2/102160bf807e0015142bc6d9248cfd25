import scala.collection.mutable.ArrayBuffer

/**
 * @author CuTs
 */
class genericQueue {
  ArrayBuffer[T] = new ArrayBuffer[T]()
var queueBody = new ArrayBuffer[T]

def enqueue[T] (item: T) {queueBody.append(item)}
def dequeue {queueBody.remove(0)}
def isEmpty = queueBody.isEmpty
override def toString = queueBody.toString()
  
}