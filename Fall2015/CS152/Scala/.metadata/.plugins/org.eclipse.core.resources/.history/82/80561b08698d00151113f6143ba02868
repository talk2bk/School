package expression
import values._

/**
 * @author CuTs
 */
class Identifier (value: String) extends Expression{
  def execute(env: Environment) = 
    if (env.contains(this)) env.get(this) else throw new Exception("identifier undefined")
}