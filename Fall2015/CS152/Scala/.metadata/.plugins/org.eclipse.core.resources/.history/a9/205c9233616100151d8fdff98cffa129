object TaxCalculator {
  
  def tax(income: Double) = {
      if(income < 0){ throw new Exception("Income can't be negative.");}
     // enter code here 
      var taxAmount = 0;
      val thirtyThousandTax = 0.05;
      val fourtyThousandTax = 0.11;
      val sixtyThousandTax = 0.23;
      val oneHundredThousandTax = 0.32;
      val richManTax = 0.50;
      if(income < 20000){taxAmount = 0;}
      else if(income < 30000){taxAmount = income;}
      else if(income < 40000){}
      else if(income < 60000){}
      else if(income < 100000){}
      else{
        
      }
    
   } 
  
    def main(args: Array[String]): Unit = {
      try {
        println("enter 3 incomes x, y, and z on separate lines:")
        var x = readDouble()
        var y = readDouble
        var z = readDouble
        println("tax(x) = $" + tax(x))
        println("tax(y) = $" + tax(y))
        println("tax(z) = $" + tax(z))
      } catch {
         case e: Exception => {println(e) }
       }
    }

}