package expression

/*
 * represents expressions of the forms:
 *      if(a) b else c
 *      if(a) b 
 */
class Conditional(condition: Expression, consequence: Expression, alternative: expression)  extends SpecialForm {
  
}